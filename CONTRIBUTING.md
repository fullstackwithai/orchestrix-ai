# Contributing

1. Create a focused feature branch.
2. Keep workflow-engine changes covered by tests.
3. Run `python -m compileall -q app tests`.
4. Run `pytest -q`.
5. Never commit `.env`, database files, credentials, or customer data.
6. Document architecture or API changes.
